# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Build output — always regenerated by `npm run build`, never hand-edited.
# Only ONE canonical output folder from now on: dist/
dist
dist-ssr
dist_*
dist-*
dist[0-9]*

*.local

# Env files — contain real secrets, never commit
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
